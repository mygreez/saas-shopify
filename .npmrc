# Configuration npm pour réduire les warnings
# Supprimer les warnings de dépendances dépréciées (transitives)
fund=false
audit=false

# Note: Les warnings de dépendances dépréciées sont normaux
# et n'affectent pas le fonctionnement de l'application.
# Elles viennent de dépendances tierces qui seront mises à jour
# par leurs mainteneurs.

